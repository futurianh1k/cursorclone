# VDE Web IDE Platform - Environment Variables
# 용도: Docker Compose 및 스크립트용 환경변수
# 주의: 실제 값으로 변경 후 사용, .env는 Git에 커밋하지 않음

# ===========================================
# 일반 설정
# ===========================================
ENVIRONMENT=poc
LOG_LEVEL=info

# ===========================================
# code-server
# ===========================================
CODE_SERVER_PASSWORD=changeme_strong_password
USER=coder

# ===========================================
# Keycloak
# ===========================================
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=changeme_admin_password
KEYCLOAK_REALM=vde-ide
KEYCLOAK_CLIENT_ID=code-server
KEYCLOAK_CLIENT_SECRET=changeme_client_secret

# ===========================================
# LiteLLM
# ===========================================
LITELLM_MASTER_KEY=sk-changeme-litellm-key

# ===========================================
# 로깅 (Langfuse - 선택적)
# ===========================================
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=http://langfuse:3000

# ===========================================
# AI 모델 설정
# ===========================================
TABBY_MODEL=StarCoder2-7B
VLLM_MODEL=deepseek-coder-6.7b-instruct
MODEL_PATH=/models

# ===========================================
# 네트워크 설정
# ===========================================
DOMAIN=ide.internal
SSL_CERT_PATH=/etc/nginx/ssl/server.crt
SSL_KEY_PATH=/etc/nginx/ssl/server.key

# ===========================================
# 스토리지 설정
# ===========================================
WORKSPACE_SIZE=20Gi
MODEL_CACHE_SIZE=50Gi

# ===========================================
# 리소스 제한
# ===========================================
CODE_SERVER_CPU_LIMIT=2
CODE_SERVER_MEMORY_LIMIT=4Gi
TABBY_GPU_COUNT=1
VLLM_GPU_COUNT=1
