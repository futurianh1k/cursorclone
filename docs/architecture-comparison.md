# 아키텍처 비교 분석: 현재 vs 목표

## 📊 개요

| 항목 | 현재 프로젝트 | 목표 아키텍처 (aivdedev) |
|------|-------------|------------------------|
| **Web IDE** | Next.js + Monaco Editor | **code-server** (VS Code) |
| **AI Autocomplete** | vLLM 직접 호출 | **Tabby** (전용 자동완성 서버) |
| **AI Chat/Edit** | 자체 구현 AIChat | **Continue** (VS Code 확장) |
| **AI Agent** | 자체 구현 (기본) | **Continue Agent** (다단계) |
| **AI Gateway** | 기본 라우터 | **정책/감사/라우팅 강화** |
| **사용자 격리** | 워크스페이스 폴더 | **1 User = 1 IDE 컨테이너** |
| **인증** | JWT 기반 | **SSO(OIDC) + MFA** |

---

## 🔄 핵심 변경점

### 1. Web IDE 전환: Monaco → code-server

**현재:**
```
Browser → Next.js → Monaco Editor (자체 구현)
```

**목표:**
```
Browser → code-server (VS Code Server)
         ├── Tabby 확장 (Autocomplete)
         ├── Continue 확장 (Chat/Edit/Agent)
         └── 내장 터미널 (빌드/디버깅)
```

**장점:**
- VS Code의 모든 기능 (터미널, 디버거, 확장 프로그램)
- Tabby/Continue 등 검증된 AI 확장 프로그램 활용
- 개발자 학습 비용 최소화 (익숙한 VS Code 환경)

---

### 2. AI 코딩 지원 아키텍처

**현재:**
```
AIChat Component → API → vLLM → Response
```

**목표:**
```
┌─────────────────────────────────────────────────────┐
│                    code-server                       │
│  ┌─────────────┐  ┌──────────────────────────────┐  │
│  │   Tabby     │  │         Continue              │  │
│  │ Extension   │  │ Extension (Chat/Edit/Agent)   │  │
│  └──────┬──────┘  └──────────────┬───────────────┘  │
└─────────┼───────────────────────┼───────────────────┘
          │                       │
          ▼                       ▼
    ┌─────────────────────────────────────────────┐
    │              AI Gateway (FastAPI)            │
    │  ┌─────────┐ ┌─────────┐ ┌────────────────┐ │
    │  │ 정책    │ │ 감사    │ │ 라우팅/로드밸런│ │
    │  │ 엔진    │ │ 로거    │ │ 싱             │ │
    │  └─────────┘ └─────────┘ └────────────────┘ │
    └─────────────────────┬───────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
    ┌──────────┐    ┌──────────┐    ┌──────────┐
    │  Tabby   │    │  vLLM    │    │ (백업    │
    │  Server  │    │  Server  │    │  모델)   │
    └──────────┘    └──────────┘    └──────────┘
```

---

### 3. 사용자별 IDE 컨테이너

**현재:**
```
1개 Web 서버 → 여러 사용자 (워크스페이스 폴더 분리)
```

**목표:**
```
사용자 로그인 → 대시보드 → 워크스페이스 생성/선택
                              ↓
              사용자 전용 IDE 컨테이너 프로비저닝
              ┌────────────────────────────────────┐
              │      User IDE Container            │
              │  ┌──────────────────────────────┐  │
              │  │      code-server             │  │
              │  │  + Tabby Extension           │  │
              │  │  + Continue Extension        │  │
              │  └──────────────────────────────┘  │
              │  ┌──────────────────────────────┐  │
              │  │    /workspace (사용자 코드)   │  │
              │  └──────────────────────────────┘  │
              └────────────────────────────────────┘
```

---

## 🏗️ 새로운 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                        사용자 브라우저                            │
└─────────────────────────────┬───────────────────────────────────┘
                              │ HTTPS
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Reverse Proxy (Traefik/Nginx)                 │
│                    - SSO 인증 (OIDC)                             │
│                    - TLS 종단                                    │
│                    - 동적 라우팅                                  │
└──────────┬─────────────────────────────────────┬────────────────┘
           │                                     │
           ▼                                     ▼
┌─────────────────────┐               ┌─────────────────────────────┐
│   Dashboard Web     │               │    User IDE Containers      │
│   (Next.js)         │               │                             │
│   - 로그인/대시보드  │               │  ┌─────────────────────┐    │
│   - 워크스페이스관리 │──생성/관리──▶│  │ user-1-ws-abc       │    │
│   - 사용자 설정     │               │  │ (code-server:4000)  │    │
└──────────┬──────────┘               │  └─────────────────────┘    │
           │                          │  ┌─────────────────────┐    │
           ▼                          │  │ user-2-ws-xyz       │    │
┌─────────────────────┐               │  │ (code-server:4000)  │    │
│   API Server        │               │  └─────────────────────┘    │
│   (FastAPI)         │               └─────────────────────────────┘
│   - 사용자 관리     │                              │
│   - 워크스페이스 API│                              │
│   - 컨테이너 관리   │                              │
└──────────┬──────────┘                              │
           │                                         │
           ▼                                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                       AI Gateway (FastAPI)                       │
│   - 정책 엔진 (Rate Limit, 파일 범위 제한)                       │
│   - 감사 로거 (프롬프트 해시, 메타데이터만 저장)                  │
│   - 모델 라우팅 (Autocomplete → Tabby, Chat → vLLM)              │
└────────────────────────────┬────────────────────────────────────┘
                             │
         ┌───────────────────┼───────────────────┐
         ▼                   ▼                   ▼
┌────────────────┐  ┌────────────────┐  ┌────────────────┐
│  Tabby Server  │  │  vLLM Server   │  │ (Fallback LLM) │
│  (Autocomplete)│  │  (Chat/Agent)  │  │                │
│  GPU 1         │  │  GPU 2+        │  │                │
└────────────────┘  └────────────────┘  └────────────────┘
```

---

## 📦 구성 요소 상세

### 1. Dashboard Web (Next.js) - 기존 유지/수정

| 기능 | 현재 | 변경 |
|------|------|------|
| 로그인 | JWT | SSO(OIDC) 연동 추가 |
| 워크스페이스 목록 | ✅ | 유지 |
| 워크스페이스 생성 | ✅ | **IDE 컨테이너 프로비저닝 추가** |
| IDE 열기 | Monaco 내장 | **code-server로 리다이렉트** |
| 설정 | 기본 | 유지 |

### 2. code-server IDE 컨테이너 (신규)

**Docker 이미지 구성:**
```dockerfile
FROM codercom/code-server:latest

# Tabby Extension 설치
RUN code-server --install-extension TabbyML.vscode-tabby

# Continue Extension 설치
RUN code-server --install-extension Continue.continue

# 설정 파일 복사
COPY settings.json /home/coder/.local/share/code-server/User/settings.json
```

**settings.json:**
```json
{
  "tabby.api.endpoint": "http://ai-gateway:8080/v1/tabby",
  "continue.serverUrl": "http://ai-gateway:8080/v1/continue"
}
```

### 3. AI Gateway (FastAPI) - 강화

**현재 구조:**
```
apps/api/src/routers/ai.py  # 기본 AI 라우터
```

**목표 구조:**
```
apps/api/src/
├── routers/
│   └── ai_gateway/
│       ├── __init__.py
│       ├── router.py          # 통합 라우터
│       ├── completions.py     # Tabby 호환 자동완성
│       ├── chat.py            # Continue 호환 채팅
│       └── agent.py           # Agent 모드
├── services/
│   └── ai_gateway/
│       ├── policy_engine.py   # 정책 엔진
│       ├── audit_logger.py    # 감사 로거
│       └── model_router.py    # 모델 라우팅
```

### 4. Tabby Server (자동완성 전용)

- **용도:** 코드 자동완성 (Inline Completion)
- **특징:** 경량, 빠른 응답 (P95 < 300ms)
- **모델:** StarCoder, CodeLlama 등 (작은 모델)

### 5. vLLM Server (Chat/Agent)

- **용도:** 채팅, 코드 편집, 에이전트
- **특징:** 대형 모델, 복잡한 추론
- **모델:** Qwen2.5-Coder-7B-Instruct 등

---

## 🔐 보안 강화

| 항목 | 현재 | 목표 |
|------|------|------|
| 인증 | JWT | SSO(OIDC) + MFA |
| 인가 | 기본 권한 | RBAC (사용자/프로젝트) |
| 격리 | 폴더 분리 | 컨테이너 + 네트워크 Namespace |
| 감사 | 기본 로그 | 중앙 수집 + 해시 기반 |
| 데이터 보호 | 기본 | 프롬프트 마스킹 정책 |

---

## 🚀 구현 우선순위

### Phase 1: 기본 WebIDE 전환 (1-2주)
1. code-server Docker 이미지 생성
2. 사용자별 IDE 컨테이너 프로비저닝 API
3. 대시보드 → IDE 연동

### Phase 2: AI 확장 통합 (1-2주)
1. Tabby Extension 설정
2. Continue Extension 설정
3. AI Gateway 확장 (Tabby/Continue 호환)

### Phase 3: 보안/운영 강화 (1-2주)
1. SSO(OIDC) 연동
2. 감사 로깅 강화
3. 정책 엔진 구현

---

## 📁 참고 문서

- `docs/aivdedev/README.md` - 프로젝트 개요
- `docs/aivdedev/PRD.md` - 제품 요구사항
- `docs/aivdedev/ARCHITECTURE.md` - 아키텍처 설계
- `docs/aivdedev/TECHNICAL_DESIGN.md` - 기술 설계
- `docs/aivdedev/AI_FEATURES.md` - AI 기능 명세
- `docs/aivdedev/AI_GATEWAY.md` - AI Gateway 설계
- `docs/aivdedev/SECURITY.md` - 보안 명세
- `docs/aivdedev/브라우저기반VSCode서버웹IDE구축가이드.pdf` - 구축 가이드
