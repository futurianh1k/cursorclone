요청자: 사용자
일자: 2026-01-07
브랜치: feature/project-workspace-split

## 요구사항 요약
- 대시보드 프로젝트 카드 상단에서 프로젝트 이름 수정/삭제 UX 제공

## Assistant(제가) 응답(무엇을 할지)
- API에 프로젝트 수정/삭제 엔드포인트를 추가하고,
- Web 대시보드에서 인라인 편집(저장/취소) 및 삭제 확인 UX를 구현,
- “워크스페이스가 있는 프로젝트는 삭제 불가”를 서버/클라이언트 모두에서 보장.

## 실제로 수행한 변경 내용(요약)
- API
  - `PATCH /api/projects/{projectId}` 추가
  - `DELETE /api/projects/{projectId}` 추가 (워크스페이스 존재 시 409)
  - 테스트 환경에서 의존성 미설치로도 앱/테스트 수집이 가능하도록 일부 미들웨어/설정에 폴백 추가
- Web
  - 프로젝트 카드 헤더: 이름 변경(인라인) + 삭제(확인) + 워크스페이스 추가 버튼 배치
  - 프로젝트가 비어 있어도 카드 표시
  - API client 함수 + vitest 테스트 추가

## 테스트 및 검증 방법
- `cd apps/web && npm test`

