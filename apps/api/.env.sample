# API 서버 환경변수 설정 예시
# 이 파일을 .env로 복사하고 필요한 값들을 설정하세요
# cp .env.sample .env

# 개발 모드 설정
# true로 설정하면 ~/cctv-fastapi를 워크스페이스로 사용합니다
DEV_MODE=true

# CORS 허용 Origin 목록 (쉼표로 구분)
# 개발 환경에서는 localhost를 허용하고, 운영 환경에서는 내부 도메인만 허용하세요
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# vLLM 서버 설정
# vLLM 서버의 OpenAI 호환 API 엔드포인트 URL
VLLM_BASE_URL=http://localhost:8001/v1

# vLLM API 키 (vLLM은 기본적으로 키가 필요 없지만, 호환성을 위해 설정)
VLLM_API_KEY=dummy-key

# 로깅 레벨 (선택사항)
# LOG_LEVEL=INFO

# 데이터베이스 (대규모 스케일링)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/cursor_poc

# Redis 캐시
REDIS_URL=redis://localhost:6379/0
REDIS_POOL_SIZE=10

# 암호화 키 (프로덕션에서는 반드시 설정!)
# 개발 환경에서는 자동 생성되지만, 프로덕션에서는 반드시 안전한 키를 설정하세요
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# MASTER_ENCRYPTION_KEY=your-encryption-key-here
