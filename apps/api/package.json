{
  "name": "@poc/api",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn src.main:app --reload --host 0.0.0.0 --port 8000",
    "lint": "python -m ruff check src || true",
    "format": "python -m black src || true"
  }
}