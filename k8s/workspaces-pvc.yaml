apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: workspaces-pvc
  namespace: cursor-poc
spec:
  accessModes:
    - ReadWriteMany  # 여러 Pod에서 동시 접근 가능
  resources:
    requests:
      storage: 100Gi
  storageClassName: nfs-client  # NFS 스토리지 클래스 (환경에 맞게 수정)
